This program will fix a tv file name based on a given regular expression and 
move the corrected file to a specified directory.

Run:
python rename.py filesToBeFixed WhereToSaveFiles

Requires a text file listing shows, specify the file 
in titleList within rename.py

An output file is also created, specify the file
in outputFileLocation within rename.py

